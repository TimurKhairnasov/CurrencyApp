<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CurrencyApp" parent="Base.Theme.CurrencyApp" />

    <style name="Theme.CurrencyApp.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.CurrencyApp.AppWidgetContainer" parent="Theme.CurrencyApp.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">16dp</item>
    </style>

    <style name="Input">
        <item name="android:background">@drawable/input_background_white</item>
        <item name="android:textColorHint">@color/black_transparent</item>
    </style>

    <style name="Animation">
        <item name="android:background">@color/white_transparent</item>
    </style>
    <style name="Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>